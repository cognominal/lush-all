- Added `SusyYamlNode` types and placeholder helpers in `lush-types`.
- Added YAML structural command logic and wired it into the YAML editor.
- Updated the YAML explore page to use the new command pipeline.
